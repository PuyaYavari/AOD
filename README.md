# AOD
Aspect Oriented Debugging for Typescript.

Add @LogFunc() above your functions to log arguments and response. You customize log messages by passing parameters to @LogFunc(), first string param is the message to write before the function call and the second string is the message to write after the function call.
